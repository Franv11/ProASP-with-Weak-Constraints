%-------------- Problem n.1 ----------------
%:- b(X), p(X).
%:- a(X) , X > 1.
%:~ p(X), a(X). [X]

%-------------- Problem n.2 ----------------
%arc(Y,X) :- arc(X,Y).
%node(X) :- arc(Y,X).
%node(Y) :- arc(Y,X).
%:~ outClique(X). [1,X]